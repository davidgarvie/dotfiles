# Fresh Install 

Install xcode dev tools (installs git):

```sh
xcode-select --install
```

Fetch and run install script from github:

```sh
sh -c "$(curl -fsSL https://raw.githubusercontent.com/davidgarvie/dotfiles/master/install.sh)"
```
